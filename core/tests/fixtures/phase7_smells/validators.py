def validate_a(x):
    """One of several distinct helpers the smelly handler calls directly."""
    return x > 0


def validate_b(x):
    """Another distinct helper the smelly handler calls directly."""
    return x < 100
